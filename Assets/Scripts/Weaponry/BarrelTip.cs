using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BarrelTip : MonoBehaviour //barreltip is manually set, determines where bullets emerge from on a weapon, class does nothing otherwise
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
